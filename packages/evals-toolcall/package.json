{
  "name": "@metaharness/evals-toolcall",
  "version": "0.1.0",
  "description": "BFCL-style function-calling / tool-use benchmark adapter for @metaharness/flywheel — evolve the tool-calling policy, not the model. Tool selection, argument normalization, schema-check verification, retry policy, calibration, and a stricter composite promotion gate.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": ["dist", "README.md"],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "lint": "tsc --noEmit"
  },
  "keywords": ["bfcl", "function-calling", "tool-use", "tool-calling", "benchmark", "eval", "flywheel", "metaharness", "policy-evolution", "llm", "agent-harness"],
  "license": "MIT",
  "dependencies": {
    "@metaharness/flywheel": "^0.1.1"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^2.0.0"
  }
}
