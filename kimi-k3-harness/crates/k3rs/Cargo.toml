[package]
name = "k3rs"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "Kimi K3 inference engine in Rust — a faithful port of kimi-k3-in-c's verified in-memory path, dependency-free"

# Detached from any parent cargo workspace: ships inside a scaffolded harness.
[workspace]

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
