# Dream Cycle Evidence Record — 2026-09-21

Deep surface: `flywheel-promotion`. Scan: `evals-verticals`, `bench`.
Session commit: `d5833dc6512ac1adeeef91a331c29055cd8a4dbb`.

Classification key: OBSERVATION | MEASUREMENT | INFERENCE | HYPOTHESIS |
DECISION | REJECTION.

## OBSERVATION

- `packages/flywheel/src/cli.ts:69,72` (pre-fix): `max = Math.max(1, ...bundle.lift_curve.map(p => p.primary))`
  floors `max` at 1 but never floors the numerator; `LiftPoint.primary`/`Score.primary` are documented only
  as "higher is better" (types.ts) with no non-negativity constraint, and `lineage.ts`'s `computeLiftCurve`
  just accumulates `rootPrimary + primaryDelta` with no clamp either.
- `String.prototype.repeat` throws `RangeError: Invalid count value` for any negative count (confirmed via
  direct `node -e` repro and via `git stash` + `npx vitest run packages/flywheel/__tests__/cli.test.ts`).
- `packages/flywheel/__tests__/` had zero test file covering `cli.ts`'s `graph`/`analyze` verbs before
  tonight (confirmed via `ls packages/flywheel/__tests__/` and grep for "graph" across the suite).
- 3 other flywheel-promotion-relevant candidates surfaced by tonight's research were deliberately NOT
  selected: (a) `gate.ts`'s `meetsPromotionRule` fails open on NaN/malformed Score fields in the LIVE path
  (gate.ts:21,23,25) — `replay.ts` already has `isCompleteScore()` for exactly this but it's never invoked
  in `run.ts`'s live promotion loop; (b) `replay.ts:151-152`'s `gateReExecutes` cross-check silently
  disables when `bundle.gate_fingerprint` is `null` and no `pinnedGateFingerprint` is supplied — same
  `verifyReplayBundle`/`gateReExecutes` family already the subject of ≥3 substantially-identical prior
  findings (08-16 #205, 08-26 #231, 08-31/09-01 #255/#258) and explicitly deferred per STEP 1.1's
  duplicate-direction rule (also 09-16's own evidence record made the same call); (c) a mixture-λ betting
  scheme for `sequential.ts`'s e-process (larger, opt-in feature, not a bug fix).
- Directly verified via GitHub (not inferred from ledger sparseness): `list_pull_requests(state=open)` on
  2026-09-21 shows **17 open PRs**, all created 2026-09-06 or later, **zero merged since 2026-09-05**
  (16 days) — 12 are `dream/*` Dream Cycle PRs (#288, #293, #298, #300, #304, #307, #314, #320, #324, #327,
  #332, #337), 5 are non-dream feature PRs (#291, #296, #302, #316, #335). This is the 4th consecutive
  night reporting this same, still-worsening crisis (09-15 #313: 7 open; 09-16 #319: 7 open, "4 days from
  STALE"; 09-20 #336: 16 open, "worse than the 08-27 8-PR crisis"; tonight: 17 open, oldest PR (#288) now
  5 days past the 14-day STALE threshold).
- `git ls-remote --heads origin "dream/*"` + `search_pull_requests` reconfirm the 2026-09-12→09-14 gap (no
  branch, issue, or PR that window) — third time this exact gap has been checked and found still
  unresolved (09-15, 09-16 previously; 09-20's issue did not re-check it explicitly).

## MEASUREMENT

- Baseline (`git stash` on `cli.ts` alone, new tests present): `packages/flywheel/__tests__/cli.test.ts`
  2/4 tests fail — `RangeError: Invalid count value: -512` and `-6`, thrown exactly at pre-fix `cli.ts:72`.
- Candidate (fix applied): `cli.test.ts` 4/4. Full `@metaharness/flywheel` suite: **76/76** (baseline suite
  size before tonight: 72; net +4, 0 regressions).
- `npx tsc -p packages/flywheel/tsconfig.json --noEmit`: clean.
- `git diff --stat`: `packages/flywheel/src/cli.ts` only, 6 insertions / 1 deletion. No other file in the
  package touched by the fix itself.
- grep confirms `@metaharness/flywheel/cli`'s `dispatch()` (which contains the fixed `graph` function) is
  imported only by `packages/create-agent-harness/src/index.ts` and `packages/avo/src/cli.ts` — no
  `evals-*` vertical or `autogenous` imports it, so the downstream blast radius is provably zero beyond
  `@metaharness/flywheel` itself.
- Independent adversarial critic (fresh subagent, cold-read, no access to the implementer's reasoning):
  reproduced the pre-fix failure and post-fix pass directly and independently; verified the clamp closes
  every input case tested (NaN, ±Infinity, `Number.MIN_SAFE_INTEGER` primaries); confirmed no test or gate
  weakened elsewhere in the repo; confirmed the new tests exercise the real `dispatch()`/`graph()` code
  path via real file I/O, no mocking. **Verdict: ACCEPT.**

## INFERENCE

- The negative-`primary` case is not contrived: a PnL/trading-style benchmark (the repo ships
  `vertical:trading` and `packages/vertical-trading`) or a root-only "honest-null" run (ADR-250's own
  encouraged outcome) can both legitimately produce a negative `primary`, and nothing upstream of `cli.ts`
  prevents it from reaching `graph`.
- This is a different bug SHAPE than the flywheel-promotion family's recent run (09-06 unreachable gate,
  09-11 noopRate ceiling lockout, 09-16 empty-array degrade) — those were all promotion-DECISION-path bugs
  (would silently mis-gate a candidate); tonight's bug is in the package's one INSPECTION/observability
  path (would crash an operator trying to look at an already-decided, already-verified bundle). Different
  risk class: not a trust/safety defect, a pure availability/robustness one.
- Given the review backlog now spans 12 dream/* + 5 non-dream PRs with zero merges in 16 days, this
  session's judgment (per STEP 1.1's "zero of last 14 merged → bias toward the smallest, most reviewable
  candidate" rule) was to select the single-file, zero-cross-package-impact fix over the two larger,
  `gate.ts`/`replay.ts`-touching findings from tonight's research, even though the NaN-fail-open finding is
  arguably higher-impact — adding a 13th PR that touches the same files 3 pending PRs already touch would
  compound the review burden, not reduce it.

## DECISION

- **Candidate selected**: `packages/flywheel/src/cli.ts`'s `graph` command — clamp the lift-curve bar-length
  calculation to `[0, 32]` so a negative-`primary` lift point renders an empty bar instead of throwing.
  1 file, 6 lines changed, 1 new test file (4 tests, all non-vacuous).
- **Verdict: ACCEPT.** All STEP 14 promotion-gate clauses satisfied at the advisory-evaluation level:
  evaluation complete, effect positive (2 previously-crashing scenarios now handled), no material
  regression (76/76, tsc clean, byte-identical output for all-non-negative bundles), reward-hack clear
  (independent critic), critic clear (ACCEPT). This session does not merge and does not autonomously
  promote any Flywheel state.
- Darwin: not run. Presentation-layer correctness fix, not a tunable routing/topology/prompt/tool/tier/
  genome parameter — no ADR-071 Darwin mutation surface applies (consistent with every prior non-parameter
  flywheel-promotion fix: 08-16, 08-26, 09-01, 09-06, 09-11, 09-16).
- ADR: none filed. A narrow, already-covered-by-existing-ADR-series correctness fix (the flywheel
  replay/receipt/lineage machinery is covered by ADR-234/235/246/249/254/274/279); not a new architectural
  decision. Checked `docs/adrs/INDEX.md` — no coverage gap this fix needs to fill.

## Process finding (disclosed, not tonight's selected candidate — see gist Recommended Next Steps)

The Dream Cycle PR review backlog is now the dominant, unresolved finding across 4 consecutive nights
(09-15, 09-16, 09-20, tonight): **17 open PRs, 0 merged since 2026-09-05 (16 days)**, oldest (#288) 5 days
past the repo's own 14-day STALE threshold. The 2026-09-12→09-14 branch/issue/PR gap remains unexplained.
Both are reported as this session's most time-sensitive findings — more so than any single code candidate —
consistent with the judgment already reached by the three preceding nights.
