# Dream Cycle Evidence Record — 2026-09-19

Deep surface: `host-adapters`. Scan surfaces: `kernel`, `sdk`. See
`2026-09-19-gist.md` for the full report; this file is the durable
classified-evidence record (STEP 13) since this repo has no flywheel
receipt CLI applicable to a non-routing/scoring host-adapter fix.

## OBSERVATION

- `packages/kernel-js/src/types.ts:41-50` documents `HarnessSpec.autonomous`
  (ADR-246 §2.2) with an explicit contract: adapters "must project this
  block per host or emit an explicit documented no-op — never silently
  drop."
- Only `packages/host-prime-agent/src/index.ts` implemented this contract
  before tonight. `host-claude-code`, `host-codex`, `host-copilot`,
  `host-opencode`, `host-pi-dev`, `host-rvm`, `host-hermes`,
  `host-openclaw`, `host-github-actions` did not reference `spec.autonomous`
  anywhere in source (confirmed via `grep -rln autonomous packages/host-*/src`
  before the fix).
- `host-github-actions/src/index.ts` already carried a commented-out
  `schedule: - cron: '0 9 * * 1'` trigger example, showing the native
  surface for `heartbeat.cadence` was known but unused.
- The remaining 4 previously-unaudited host adapters (copilot/opencode/
  pi-dev/prime-agent) render via markdown or `JSON.stringify`, not raw
  YAML/shell templating — no 7th instance of the ADR-046
  unescaped-interpolation bug class found.

## MEASUREMENT

- Round-1 red proof (8 packages, `git stash` on `src/index.ts` only, tests
  kept): each package showed exactly 1 new assertion fail non-vacuously,
  0 unrelated regressions.
- Round-2 red proof (`host-github-actions` alone, post-critic revision):
  7/33 new assertions fail non-vacuously pre-fix, 26/33 baseline
  unaffected.
- Final green state: 192/192 tests across the 9 patched packages (per-file
  counts in the gist), 0 regressions. `tsc --noEmit` clean on all 9.
  `create-agent-harness` multi-host integration suite: 572/574 (2
  pre-existing skips), unaffected.
- `npm audit --omit=dev --audit-level=high --json`: 0 high/critical.
- Diff: 18 files, +325/-4 lines.

## INFERENCE

- The disclosure-only fix is the correct remedy for 8/9 hosts (no native
  autonomous-loop surface exists to project onto); GitHub Actions is the
  one host where real projection was both available and low-risk, per the
  independent critic's round-1 finding.
- The `looksLikeCron()` guard (5-field heuristic) is a deliberate
  under-approximation: it will not validate full cron field semantics, only
  reject obviously non-cron strings (e.g. "every 5 minutes"), to avoid
  emitting a value GHA would reject at workflow-parse time while not
  overclaiming full cron validation.

## DECISION

`ACCEPT` — human review requested, not autonomous promotion. See gist
"Recommended Next Steps" for 3 deferred follow-ups (hermes/rvm real-cadence
research, MCP 2026-07-28 conformance tests, host-config.ts parity test).

## REJECTION

None. No hypothesis or candidate direction was rejected tonight; the
architecture-review agent's weaker candidates (`host-config.ts` parity
drift, missing MCP transport discriminator) were deprioritized by the
STEP 3.2 scoring rubric, not rejected as false — both carried forward as
recommended next steps above, not as failed findings.
