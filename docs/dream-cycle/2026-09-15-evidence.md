# Dream Cycle 2026-09-15 — Flywheel Evidence Record (generator-genome)

Classification key: OBSERVATION | MEASUREMENT | INFERENCE | HYPOTHESIS | DECISION | REJECTION

## OBSERVATION
`packages/create-agent-harness/src/genome-scorers.ts::resolveAgentTopology` gated
the `'tester'` agent-role recommendation on `profile.testCommands.length > 0 ||
profile.hasCi`. `testCommands` is populated in `analyze-repo.ts` (lines
177-198) from language detection alone: any repo where `languages` includes
`rust` gets `'cargo test'` pushed unconditionally, and any repo where
`languages` includes `python` gets `'pytest'` pushed unconditionally —
independent of whether any real test file exists on disk.

## OBSERVATION
The sibling scorer `scoreTestConfidence` (same file) already gates on
`profile.hasVerifiedTestFiles` (real `tests/`/`__tests__/`/`test` directory
scan evidence from `analyze-repo.ts` line 212), fixed for exactly this
class of gap by Dream Cycle 2026-08-25 (#229, merged). `resolveAgentTopology`
was not updated in lockstep and remained on the pre-#229 shape.

## MEASUREMENT (baseline, git-stash isolated)
`git stash` on `genome-scorers.ts` alone (new tests present, source
unpatched): `packages/create-agent-harness/__tests__/genome-scorers.test.ts`
→ 34 passed / 1 failed. The 1 failure is non-vacuous:
`expected [ 'maintainer', 'tester' ] to not include 'tester'` — the exact
predicted pre-fix behavior for a python repo with `hasVerifiedTestFiles:
false, hasCi: false`.

## MEASUREMENT (candidate)
Same file, patched: 35/35 passed.
Full `create-agent-harness` package suite — true baseline (`git stash` on
both changed files): 572 passed / 2 skipped (574). Candidate: 574 passed /
2 skipped (576). +2 net new tests, 0 regressions.
`tsc --noEmit -p packages/create-agent-harness/tsconfig.json`: clean before
and after. `npm run build --workspace=metaharness`: clean.

## MEASUREMENT (live CLI, real built dist/, two independent fixtures)
Fixture A — `/tmp/smoke-py-notests`, a `pyproject.toml`-only directory, no
`tests/` dir, no CI: `node dist/bin.js genome . --json` →
`agent_topology: ["maintainer","security"]` (no `"tester"` — matches the
fix).
Fixture B — same directory, `tests/test_smoke.py` added: →
`agent_topology: ["maintainer","tester","security"]`, `test_confidence`
0 → 0.5 (the sibling `scoreTestConfidence` scorer moving in lockstep, as
expected — it was already using the real-evidence signal).

## INFERENCE
`resolveAgentTopology` was the last live, untested, user-visible sibling of
the #229 bug class reachable from `harness genome`'s exported
`agent_topology` field (the only production call site, confirmed by
`grep`). The fix closes it without touching `scoreTestConfidence`,
`scoreMcpRisk`, `classifyRepoType`, or `scorePublishReadiness` (each
independently green, unmodified).

## DECISION
ACCEPT-class evidence: non-vacuous red/green proof, 0 regressions, live-CLI
verified on 2 independent fixtures, isolated to 1 source file + 1 test
file, single call site. Recommended for human review as a small,
deterministic correctness fix. Not autonomously promoted — this session
never merges and never promotes Flywheel state.

## Disclosed, not fixed tonight (sibling gaps found during the same pass,
kept out of scope to preserve tonight's small-diff bias — see gist and
issue for the zero-merged-PR-backlog context this bias responds to)

- `scorePublishReadiness` (genome-scorers.ts:131): `s += 0.15` for
  `profile.testCommands.length > 0` — the identical manifest-trust
  pattern, one function over. Tied top candidate-score with tonight's
  selected fix (see gist §Hypothesis); not selected because it's one of
  5 additive terms in a numeric blend (diluted end-user visibility) vs.
  `agent_topology`'s directly-exported role list.
- `repo-scorecard.ts` `compileConfidence`/`taskCoverage` (lines ~70-92,
  ADR-041 `metaharness score <repo>` command): same pattern, plus an
  existing asserted fixture (`__tests__/repo-scorecard.test.ts:44`) that
  currently locks in the bug (`compileConfidence >= 80` for a repo with a
  declared test script and zero real test files). Also touches a file PR
  #304 (open, unmerged, `toolSafety` fix) already modifies — a second
  independent fix here would create a 2-way merge-order dependency on
  that file; flagged rather than risked tonight.
- `genome.ts::formatGenomeReport` section 5 (`testable: yes/no`,
  line ~152): same distrusted signal, but display-only text — no scored
  field is affected. Lowest-priority of the four.
- `scoreMcpRisk`'s `local_permissive` branch (genome-scorers.ts:69-77) is
  structurally unreachable through the real CLI: `analyze-repo.ts:282`'s
  `recommendPlan()` always assigns `policy: SAFE`, a single hardcoded
  constant, so `plan.policy`'s `allowShell`/`allowNetwork`/
  `allowFileWrite`/`!defaultDeny` checks can never be true in production —
  `mcp_surface` can only ever report `local_default_deny` for `mcp:
  'local'`, regardless of a repo's actual risk profile. This is the same
  root function (`recommendPlan`'s hardcoded constant) already
  responsible for PR #304's disclosed "`mcp` mode never recommends
  `'off'`" limitation — a second, independent constant-output gap in the
  same function. Structurally the strongest runner-up candidate tonight
  (candidate-score ~4.5 vs. 4.65 selected, see gist), not selected
  because a real fix touches `recommendPlan()`'s shared output, consumed
  by multiple other scorers (`scorePublishReadiness`'s `defaultDeny`
  credit, PR #304's `toolSafety` reuse of `scoreMcpRisk`) — a
  meaningfully larger blast radius than tonight's single-call-site fix.
  Good candidate for a future `generator-genome` night, possibly meriting
  its own short design note given it's now a 2-for-2 pattern in
  `recommendPlan()`.
