# generator-genome SOTA Report — 2026-08-15

**TL;DR**: 2026 practitioner tooling has spun "agent/repo readiness scoring"
into its own small category (Factory.ai, kodustech, agent-next, and others,
all launched Jan–May 2026) — and the one rigorous academic check of this
exact space (ETH Zurich's AGENTS.md study, Feb 2026) found a widely-rewarded
readiness signal actually *reduces* agent success, meaning nobody has
validated these heuristics against real outcomes yet, MetaHarness included.
Auditing MetaHarness's own `harness genome` scorer against that lens found a
concrete, previously-undiscovered instance of exactly this problem:
`scorePublishReadiness` credited only `typescript`/`rust` in its language
term, and `analyze-repo.ts` never synthesized a build command for
python/go — so a fully mature (CI-wired, tested, buildable) python or go
repo capped at 0.40 publish-readiness and could **never** reach `harness
genome`'s 0.75 `'ready'` threshold, independent of actual quality. Fixed,
independently critiqued, and backed by the module's first-ever dedicated
unit test suite (it had zero before tonight despite its own header claiming
"unit-testable without fixtures").

## What's New in 2026

| Finding | Source | Confidence |
|---|---|---|
| "Agent Readiness" is now a real, fragmented tooling category outside the major orchestration frameworks — Factory.ai (proprietary, 8-pillar/5-level), kodustech/agent-readiness (OSS equivalent), agent-next/agent-ready (DevEx-framework-grounded, public leaderboard) — none of it absorbed into LangGraph/AutoGen/CrewAI/Agents-SDK/DSPy itself | [factory.ai/news/agent-readiness](https://factory.ai/news/agent-readiness), [github.com/kodustech/agent-readiness](https://github.com/kodustech/agent-readiness), [Haolin Dai launch post](https://medium.com/@haolindai/introducing-agent-readiness-scoring-repos-for-the-agent-era-11cbfa2dbb7c) | B (vendor/practitioner claims, cross-checked across 3+ independent projects) |
| ETH Zurich's AGENTbench (138 real-world tasks) found LLM-generated AGENTS.md context files — a signal nearly every 2026 readiness tool rewards — *reduce* agent success ~3% while adding cost; the strongest evidence that current readiness heuristics are unvalidated against ground truth, not just MetaHarness's | Gloaguen et al., arXiv:2602.11988 (Feb 2026), cross-checked via MarkTechPost + InfoQ | A for the existence/headline finding (peer-reviewable, cross-checked by 2 outlets); B for the exact -3% figure |
| Repo-level characteristics are empirically strong, language-skewed predictors of coding-agent success: compiled-language repos (C++) resolve ~9.5% vs Python ~41% on SWE-bench-style tasks; solution leakage explains ~33% of nominal "successes" | SWE-Bench++, arXiv:2512.17419; Scale SWE-bench Pro leaderboard | A (arXiv preprint + independently reported leaderboard numbers align) |
| "ai-harness-scorecard" (OSS, pip-installable) is the closest direct naming/framing peer to MetaHarness's own scorecard found anywhere — 31 checks, 5 weighted categories, explicitly built on DORA 2025 + OpenAI's Harness Engineering post | [github.com/markmishaev76/ai-harness-scorecard](https://github.com/markmishaev76/ai-harness-scorecard) | C (single-author project, methodology claims not independently reproduced) |

## MetaHarness Current Capability

`harness genome`/`score`/`doctor` and `metaharness score` (ADR-041) are
three parallel, unreconciled scoring subsystems over static file presence —
none learned, none calibrated against a scaffold-and-run outcome.
`genome-scorers.ts` backs `harness genome` with 5 pure, deterministic
functions; its own header calls itself "unit-testable without fixtures,"
yet had **zero dedicated tests** anywhere in the package before tonight —
confirmed by grepping every test file for each function name. That gap hid
tonight's finding: `scorePublishReadiness`'s language credit was a
`typescript`/`rust` allow-list, and `buildCommands` inference in
`analyze-repo.ts` (shared with `apps/web-ui`'s browser importer) generated
a build step only for those two languages, even though `python`/`go` are
first-class archetype languages (`data-pipeline-harness` requires python).
The sibling `metaharness score` command's taskCoverage dimension had an
analogous "measures the recommendation, not the repo" bug, fixed
2026-08-14 in #196/#171 — tonight's finding is the same failure class in
the neighboring `harness genome` surface.

## Competitor Comparison

| System | Repo-readiness / agent-fit scoring? | Mechanism | Grade |
|---|---|---|---|
| LangGraph (LangSmith) | No | Project templates only, no pre-flight scoring | B |
| AutoGen (maintenance-mode, → MS Agent Framework) | No | AutoGen Bench scores post-hoc *agent* performance, not repo readiness | B |
| CrewAI | No | `deploy --prepare` lints deployability, not task-fit | B |
| OpenAI Agents SDK / Codex | No direct equivalent | Static `AGENTS.md`; "Harness Engineering" post gives qualitative pillars, no numeric score | B |
| Claude Agent SDK / Claude Code | No official equivalent | `/init`/`/doctor` are static-context/settings-health, not a fit predictor | B |
| DSPy / GEPA | No | Optimizes given a task/metric; repo readiness out of scope | B |
| Factory.ai "Agent Readiness" (closer comparison) | Yes | 8-pillar/5-level, proprietary, cloud-executed, includes Observability + Task Discovery — dimensions MetaHarness's genome scorer has none of | B |
| ai-harness-scorecard (closer comparison) | Yes | 31 checks/5 categories, OSS, closest name/framing match to MetaHarness's own "scorecard" language | C |

The 6 mainstream frameworks are mostly "No" because readiness-scoring split
off as its own adjacent tooling category (Jan–May 2026) rather than being
absorbed into any orchestration framework — a genuine, still-fragmented gap,
not a solved-and-hidden one. None of the 8 systems reviewed — MetaHarness
included, per tonight's finding — has validated its heuristics against
actual downstream task success.

## Hypothesis

Given `genome-scorers.ts`'s `scorePublishReadiness(profile, plan)` and
`analyze-repo.ts`'s `buildCommands` inference, when the language-credit term
is generalized from a `typescript`/`rust` allow-list to "any detected
language" and `buildCommands` gains a python (`pip install -e .` or
`pip install -r requirements.txt`) and go (`go build ./...`) entry, then a
fully mature python or go repo (CI-wired, tested, buildable, default-deny
policy) should score `publish_readiness >= 0.75` — reaching `harness
genome`'s `'ready'` verdict threshold, matching an equivalently mature
typescript repo — subject to: (a) typescript/rust scores do not regress,
(b) all 5 `genome-scorers.ts` functions gain direct unit coverage (zero
existed before), (c) no existing test is weakened, (d) the diff stays
materially under 300 lines.

## Benchmarks / Evaluation

`evaluated: accepted`

Deterministic, non-sampled before/after repro via the new test suite
(`packages/create-agent-harness/__tests__/genome-scorers.test.ts`, 18
tests, all 5 scorer functions covered):
- **Pre-fix** (source unchanged, test file added): **16 passed, 2 failed**
  — exactly the 2 predicted failures (`a fully mature python/go repo now
  reaches ready-threshold readiness`), both failing on
  `profile.buildCommands.length > 0` before even reaching the score
  assertion — non-vacuous.
- **Post-fix**: **18/18 passed**.
- Full package suite (`packages/create-agent-harness/__tests__/`):
  **476 passed, 2 skipped** (458 passed pre-existing + 18 new; the 2 skips
  are pre-existing `.real.test.ts` platform-gated tests, unaffected) — zero
  regressions.
- `npx tsc --noEmit -p packages/create-agent-harness/tsconfig.json`: clean.
- Full repo `npm run build`: clean, `DONE in 24011ms`, full JS/TS workspace
  (no Rust/wasm/NAPI step reached or needed for this candidate).
- Diff size: 2 source files (20 insertions / 4 deletions) + 1 new test file
  (175 lines) — well under the 300-line target.

## Adversarial Critique

Independent critic (no authorship context) reviewed the diff, re-derived
the pre-fix failure by reverting the two source edits and rerunning the
new test file, checked for second-order effects of rust's language credit
rising from 0.15→0.3, checked whether `pip install -e .`/`go build ./...`
are safe universal claims, attempted to construct a cheating implementation
that would pass the new tests without fixing the bug, and checked the
`apps/web-ui` sibling-gap disclosure for accuracy. See the linked issue/PR
for the full critique and verdict.

## Darwin Results

Not run — this is a scorer-correctness fix (a language-credit allow-list
bug), not a tunable routing/topology/prompt/tool/tier parameter; no Darwin
mutation surface applies (ADR-071's mutation allowlist), same reasoning as
2026-08-13 and 2026-08-14's entries.

## SOTA Proof & Witness

Evidence rung: a deterministic, 100%-reproducible before/after test-suite
repro (16 failing → 18 passing on the exact predicted assertions), not a
statistical or live-model claim. `data_source: deterministic-repro`.
`LLM_EVAL=available` this session (`OPENROUTER_API_KEY` present) but
tonight's candidate needed zero model calls. See Witness below for the
final stamp.

## Recommended Next Steps

1. **Port the identical fix to `apps/web-ui/src/generator/repo.ts`** — the
   browser-side Studio importer (ADR-023/026) duplicates
   `analyze-repo.ts`'s exact pre-fix `buildCommands` logic (python/go get no
   build command there either). Not touched tonight to keep the diff
   confined to one package/surface; the web-UI has no `scorePublishReadiness`
   equivalent today, so only the build-command half of tonight's fix
   applies there.
2. **Fix `mcp:'off'` being structurally unreachable** — every
   `analyze-repo.ts` archetype is `mcp:'local'`/`'remote'`, so
   `resolveAgentTopology`'s `'security'` role and `scoreMcpRisk`'s
   non-zero-risk branch fire for every repo, MCP or not — inflating
   `risk_score` by a fixed ~0.045 regardless of actual MCP usage. Natural
   next DEEP candidate: a real no-MCP archetype, or decouple the decision
   from the archetype's static default.
3. **Fix `scoreTestConfidence` measuring language, not tests** —
   `testCommands` fires unconditionally from language detection
   (`cargo test`/`pytest`/etc.), never from scanning for a real
   `tests/`/`__tests__/` directory, though `score.ts` already implements
   that check and could be reused. A repo with zero test files can still
   score 0.5–0.7 "test confidence."

## Witness

```
session_commit : 5f1d4b244cff5af0ec9b147a3f57f615b45abc22  (HEAD @ start of run, after correcting a stale local main ref)
report_sha256   : PENDING
witness         : PENDING
evaluation      : packages/create-agent-harness/__tests__/genome-scorers.test.ts
                  16→18/18 (2 predicted pre-fix failures, non-vacuous);
                  full package suite 476/478 (2 pre-existing skips,
                  unaffected), 0 regressions; tsc clean; full repo build
                  clean. Deterministic repro, no live model call this run.
darwin          : not run (no applicable mutation surface — see Darwin Results)
flywheel        : not applicable (generator-genome is not a flywheel domain)
```

No gist-creation tool is available in this session's toolset (GitHub MCP
server has no `create_gist` equivalent; this environment has no `gh` CLI /
direct API access — same as 2026-08-13/2026-08-14). This committed file is
the canonical, witnessed artifact for tonight's run; `GIST=LOCAL`. Verifier
procedure: take this file as committed, replace the `report_sha256` line's
value with the placeholder text above and the `witness` line's value with
its placeholder, SHA256 the resulting file — must equal the committed
`report_sha256`. Then concatenate `report_sha256 + session_commit` and
SHA256 again — must equal the committed `witness`.
