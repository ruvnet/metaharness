# Dream Cycle 2026-08-26 — Flywheel Evidence Record

Durable evidence for tonight's flywheel-promotion candidate, classified per
the Dream Cycle knowledge-type discipline.

## OBSERVATION

`verifyReplayBundle`'s `gateReExecutes` (packages/flywheel/src/replay.ts, pre-fix)
constructed `PromotionEvidence` as `{ baseline: c.baselineScore, candidate:
c.candidateScore }` only — `evidence.anchor` was never set. `meetsPromotionRule`'s
anchor clause (`e.anchor && e.anchor.candidate < e.anchor.baseline`) was therefore
unreachable during replay. Confirmed by direct code inspection and independently
by a fresh architecture-review subagent with no shared context (file:line citations
match).

## MEASUREMENT

- Pre-fix repro: stashing only `replay.ts` (keeping the new tests) produced exactly
  1 non-vacuous failure (`an ANCHOR-REGRESSED promotion ... → gateReExecutes false`:
  expected `false`, got `true`) — the predicted failure, nothing else.
- Post-fix: `packages/flywheel` suite 51/51 green (47 pre-existing + 4 new).
- `tsc --noEmit` clean (package-scoped lint == tsc --noEmit here).
- 7 real committed `ReplayBundle`s re-verified against the built, patched dist:
  `packages/radio/.radio-flywheel/replay-bundle.json`,
  `kimi-k3-harness/.harness/flywheel/replay-bundle.json`,
  `packages/darwin-mode/bench/swebench/proof-bundle-swebench.json`,
  `experiments/signal-flywheel/bundle.json` (also its own `verify.mjs`: PASS),
  `packages/evals-math/bench/proof-bundle-gsm8k*.json` (3 files) — all 7 `pass: true`.
  The 2 using the default gate (darwin-swebench, signal-flywheel) both show
  `gateReExecutes: true` with the anchor clause now actively exercised — no
  false-positive regression on any real evidence in the repo.

## INFERENCE

The fix genuinely closes the disclosed gap (an anchor-regressed promotion now
fails replay) without weakening the trust model elsewhere, because it matches
the anchor axis to the exact same sealed-field trust tier the codebase already
uses for `baselineScore`/`candidateScore` — verified by tracing the Ed25519
signer payload shape (`run.ts` sign() calls) and confirming none of the three
axes were ever signature-covered, before or after this patch.

## HYPOTHESIS (frozen before implementation, STEP 3.3)

See `docs/dream-cycle/2026-08-26-gist.md` §Hypothesis — verbatim, unmodified
after evaluation began.

## DECISION

`evaluated: accepted`. Recommendation: **ACCEPT** (human review still required
before merge — this run never promotes itself). All STEP 14 promotion-gate
criteria met: evaluation complete, effect positive (closes a real replay-trust
gap), no material regression (51/51 + 7/7 real bundles), tests green,
reward-hack clear, independent critic ACCEPT, receipt reproducible
(deterministic, not sampled). Witness below.

## REJECTION (adjacent, explicitly not pursued tonight)

Candidate B from research (signing `failureReasons`/full `Score` objects into
the receipt payload — the second gap PR #205 disclosed) was scored lower on
production value (diagnostic-ledger honesty only, doesn't affect the
PROMOTED-chain trust boundary) and reviewability (touches more call sites) than
candidate A. Not rejected as wrong — deferred, disclosed as next-night work in
the gist and issue.

## Witness

```
session_commit : 44fbcdd6e84a2d3b9c4daaf4d1a2372f9eb1cd7c
gist           : docs/dream-cycle/2026-08-26-gist.md (witness stamped there)
```
