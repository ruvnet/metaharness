# MetaHarness Dream Cycle — Ledger

Durable memory across nightly Dream Cycle runs. One row per night. See
`docs/dream-cycle/${DATE}-gist.md` (or the published gist) and the linked
issue/PR for full detail.

| Date | Deep | Finding | Issue | PR | Evaluated? | Verdict | Effect | Witness | Prior-night fates |
|---|---|---|---|---|---|---|---|---|---|
| 2026-08-13 | security-adversarial | Added `indirect_prompt_injection` (OWASP LLM01 indirect, CVE-2025-32711-backed) as redblue's 6th attack family; independent critic caught+fixed a keyword-collision confound before ship | #180 | #181 | yes | ACCEPT-WITH-CAVEATS | live spend $0.00042, 0/6 offline-suite regression, vacuous true-negative on the new family (target has no tool-channel model) | `398c71a6...` | n/a (first ledger entry) |
| 2026-08-14 | host-adapters | Fixed hermes YAML mapping-key injection (`AgentSpec.name`/`HarnessSpec.name` unescaped into `agent.personalities.<name>:`) in all 3 byte-for-byte-parity codegen paths (adapter, CLI scaffold, web-UI); critic caught+fixed a reserved-bare-scalar hazard (`true`/`null`/`123`) pre-ship and flagged 2 still-open sibling gaps (comment-line + MCP-value injection), disclosed not hidden | #187 | #188 | yes | ACCEPT-WITH-CAVEATS | 19+15+18/18 tests green (13→19, 13→15, 16→18), 0 regressions across 454+69 unaffected full-suite tests, deterministic before/after repro (no live spend) | `1e859efa...` | #180/#181 MERGED |
| 2026-08-15 | generator-genome | `scorePublishReadiness` generalized language-credit from a typescript/rust allow-list to any detected language, plus python/go `buildCommands` inference; added the first unit suite for `genome-scorers.ts` (19 tests, zero existed before) | #199 | #200 | yes | ACCEPT-WITH-CAVEATS | 16→19/19 new tests non-vacuous, 477 full-suite 0 regressions, rust readiness 0.15→0.30 side-effect independently verified to cause no verdict-threshold flip | `b8c11365...` | #187/#188 MERGED |
| 2026-08-16 | flywheel-promotion | `verifyReplayBundle` extended receipt verification from the promoted `chain` to the full `all_commits` diagnostic ledger, and `gateReExecutes` now fails (not silently skips) an unauditable promoted commit; independent critic caught a receipt-splicing id/verdict-flip forgery gap pre-ship, closed with `receiptMatchesCommit()` | #204 | #205 | yes | ACCEPT-WITH-CAVEATS | flywheel suite 47→52/52, 0 regressions, 4 real committed replay bundles re-verified PASS on patched dist | `c28913c0...` | #200 OPEN unreviewed; #187/#188 MERGED |
| 2026-08-17 | darwin-evolution | Wired ADR-249's dormant `signals.cost` seam into `evolve()`'s only production call site via new opt-in `EvolutionConfig.costBudgetBytes` (byte-identical when unset); crafted near-tie fixture shows the cost decay alone can flip a promotion decision | #206 | #207 | yes | ACCEPT-WITH-CAVEATS | darwin-mode suite 632/632 (0 regressions, 14 pre-existing skips), 2 dependent packages 11/11 | `57ed9167...` | #200/#205 OPEN unreviewed; #187/#188 MERGED |
| 2026-08-18 | security-adversarial | `mcp-scan`'s `risky-bash-allow` regex extended to arbitrary-code interpreters (python/node/ruby/perl/bash/sh) + new `unrestricted-bash-allow` check; a real `metaharness --template vertical:ai` scaffold's shipped `Bash(python *)` grant was invisible to the scanner before this fix | #209 | #210 | yes | ACCEPT-WITH-CAVEATS | 455→457/457 0 regressions, real-CLI before/after repro, false-positive check confirms scoped rules stay unflagged | `97587f9c...` | #200/#205/#207 OPEN unreviewed; #187/#188 MERGED |
| 2026-08-19 | host-adapters | `github-actions` codegen sibling of #188's injection class: `shellDq()`+`yamlCommentSafe()` applied across all 3 byte-parity paths; 3 independent adversarial critic rounds, each catching a real gap the previous round (including the author) missed | #211 | #212 | yes | ACCEPT-WITH-CAVEATS | 460/462 + 24/24 + 71/71, 0 regressions; real bash execution proved live pre-fix command injection and full post-fix inertness | `b80c1045...` | #200/#205/#207/#210 OPEN unreviewed; #187/#188 MERGED |
| 2026-08-20 … 2026-08-23 | — | **Gap: no `dream/*` branch or PR exists for these 4 dates.** Cause unconfirmed — not yet root-caused by any session; flagged here rather than silently skipped over. First gap since tracking began 2026-08-13. | — | — | — | — | — | — | — |
| 2026-08-24 | host-adapters | `host-rvm`'s `install-rvm.sh` shell+comment injection — genuine RCE, the 4th and most severe sibling of the #188 bug class (config corruption vs. live command execution); `shellDq()`+`commentSafe()` at all 4 sites, 2 critic rounds (round 1 found an unescaped `#`-comment newline breakout the initial fix missed) | #223 | #224 | yes | ACCEPT-WITH-CAVEATS | host-rvm 41/41, 0 regressions; real `bash -c` execution proved live RCE pre-fix (marker file created) and full inertness post-fix | `fc5f47bc...` | #200/#205/#207/#210/#212 OPEN unreviewed (5 of 5 dream-cycle PRs to date) — explicitly disclosed in-PR as tonight's real bottleneck |
| 2026-08-25 | generator-genome | `scoreTestConfidence` credited `testCommands` (manifest/language detection only) regardless of whether real test files exist; gated on new `hasVerifiedTestFiles` filesystem scan mirroring `score.ts`'s existing `dirExists` pattern | #228 | #229 | yes | ACCEPT-WITH-CAVEATS | 464→466/468 (2 pre-existing skips), 0 regressions; 4/6 new tests non-vacuously failed pre-fix | `d53d9cf6...` | 6 PRs open/unreviewed (#200/#205/#207/#210/#212/#224); PR body explicitly named the review bottleneck, not more findings, as the real constraint |
| 2026-08-26 | flywheel-promotion | `gateReExecutes` re-executed the promotion gate on sealed baseline/candidate scores but never supplied `evidence.anchor`, leaving the anti-Goodhart anchor clause structurally unreachable during replay — the exact gap #205 (2026-08-16) had disclosed-not-fixed; closed by reading sealed `anchorScore` off root + promoted commits | #230 | #231 | yes | ACCEPT | flywheel 47→51/51, 0 regressions; 7 real committed replay bundles re-verified PASS on patched dist; independent fresh-subagent critic returned ACCEPT | `2b513a58...` | 7 PRs open/unreviewed at open. Same-day (08-27 07:07 UTC) an independent review found a fail-open gap in the score-completeness check; addressed same-day via 2 follow-up pushes, now 62 tests |

**Ledger-integrity note (added 2026-08-27):** Rows for 2026-08-15 through 2026-08-26 above were backfilled tonight from GitHub PR/issue state, not written on their original nights. STEP 25 ("update the ledger") silently failed for 8 consecutive Dream Cycle runs even though every one of those runs otherwise completed its full pipeline (research → hypothesis → candidate → evaluation → critique → draft PR) — the PRs and issues existed and are the source for this backfill, but the durable-memory row never landed. This means STEP 1's ledger-based learning signals (duplicate-direction detection, the zero-merge bias rule, the review-bottleneck rule) were operating on a table that looked like only 2 nights had ever run, for two weeks, even as 8 real nights of evidence accumulated on GitHub. The individual nightly PRs partially self-corrected by reading GitHub state directly instead of the ledger (see e.g. #229, #231's own bodies), which is why the pipeline's actual output stayed sound — but the intended durable-memory mechanism itself was silently broken this whole time. Root cause not diagnosed (could be a `gh`/tool-availability difference across sessions, a step ordered after something that then aborted, or the step simply being skipped) — flagging for the human rather than guessing further.
